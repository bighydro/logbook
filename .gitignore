.venv/
__pycache__/
*.pyc
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
.env
data/
.DS_Store

# never commit a personal record into the code repo
/logbook.json
/logbook/[0-9][0-9][0-9][0-9]/
/notes/
/inbox/
site/

# day packages written by `logbook export --day` are views, not the record
export/

# where `logbook sync` left off per source: bookkeeping, not the record
state/
